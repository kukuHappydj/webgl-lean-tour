{
  "name": "webgl-test",
  "version": "1.0.0",
  "description": "WebGL TypeScript project",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
